{
  "manifest_version": "0.3",
  "name": "mcp-company-change-event-feed",
  "version": "1.0.0",
  "description": "MCP server that monitors a company domain for changes across hiring, tech stack, funding, firmographics, and social since the last run, returning only the deltas as typed change events in flat Clay-ready JSON.",
  "author": {
    "name": "Mamba Labs"
  },
  "license": "MIT",
  "homepage": "https://github.com/mambalabsdev/mcp-company-change-event-feed",
  "repository": {
    "type": "git",
    "url": "https://github.com/mambalabsdev/mcp-company-change-event-feed"
  },
  "server": {
    "type": "node",
    "entry_point": "build/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/build/index.js"
      ],
      "env": {
        "APIFY_TOKEN": "${user_config.apify_token}"
      }
    }
  },
  "user_config": {
    "apify_token": {
      "type": "string",
      "title": "Apify API Token",
      "description": "Apify API token for running the actor",
      "sensitive": true,
      "required": false
    }
  }
}
